# server options
# OPTIONS="-C /etc/conserver/conserver.cf -p 33000 -d -U/var/log/conserver/to_syslog.fifo -L/var/log/conserver/server.log"
# run as different user that root
#RUNAS=conservr
<% @merged_init_config_hash.sort.map do |k,v| -%>
<%=k%>=<%=v%>
<% end -%>
